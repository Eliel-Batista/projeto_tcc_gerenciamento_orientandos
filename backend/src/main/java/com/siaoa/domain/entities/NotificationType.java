package com.siaoa.domain.entities;

/**
 * Notification types used in the system.
 */
public enum NotificationType {
    INFO,
    WARNING,
    SUCCESS,
    ERROR,
    REMINDER,
    LINK_REQUEST,
    MEETING_REQUEST,
    MEETING_CANCELLED
}
